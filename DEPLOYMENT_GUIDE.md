# 🚀 Robot Face Controller - 部署指南

## 快速开始（3步）

### 1️⃣ 准备环境
- 安装 Node.js（LTS版本）：https://nodejs.org/
- 验证安装：打开命令行，运行 `node -v`

### 2️⃣ 复制到目标电脑
将整个程序文件夹复制到目标电脑（保持文件夹结构不变）

### 3️⃣ 运行程序
**双击 `RUN.bat` 文件** ✨

程序会：
- ✅ 检查环境
- ✅ 启动服务器
- ✅ 自动打开浏览器

## 📋 系统要求

| 项目 | 要求 |
|------|------|
| **操作系统** | Windows 7 或更高版本 |
| **Node.js** | v16.0.0 或更高版本 |
| **硬盘空间** | ~300MB |
| **内存** | 最少 512MB |
| **浏览器** | 任何现代浏览器（Chrome、Firefox、Edge等） |

## 🆘 常见问题

### 问题1：提示"Node.js未安装"
**解决方案：**
1. 访问 https://nodejs.org/
2. 下载 LTS 版本
3. 安装时选择"Add Node.js to PATH"
4. 重启电脑
5. 重新运行 `RUN.bat`

### 问题2：端口3000被占用
**症状：** 启动时报错 "EADDRINUSE"

**解决方案：**
- 关闭其他使用端口3000的程序
- 或修改 `server.js` 中的 PORT 变量

### 问题3：程序启动后浏览器未打开
**解决方案：**
- 手动打开浏览器
- 访问：http://localhost:3000

### 问题4：提示"dist文件夹不存在"
**解决方案：**
- 确保文件完整解压
- 检查是否有 `dist` 文件夹
- 如没有，在管理员命令行运行：`npm run build`

## 📂 文件结构说明

```
robot-face-controller/
├── RUN.bat                 ⭐ 启动脚本（双击运行）
├── server.js              📱 生产服务器
├── dist/                  📦 编译后的程序文件
│   ├── index.html
│   └── assets/
├── package.json          🔧 项目配置
├── node_modules/         📚 依赖包（重要）
└── ...其他文件
```

## 🔧 进阶操作

### 更改端口
编辑 `server.js`，修改：
```javascript
const PORT = 3000;  // 改为其他端口，如 8080
```

### 更改默认打开浏览器
需要手动修改 `RUN.bat` 中的最后一行添加浏览器启动命令

### 卸载程序
直接删除整个文件夹即可（无注册表更改）

## 📝 版本信息
- **程序名称：** Robot Face Controller
- **版本：** 1.0.0
- **打包日期：** 2026年4月
- **技术栈：** React + Vite + Node.js

## 💬 技术支持

如有问题，请检查：
1. Node.js 是否正确安装
2. 文件是否完整（有dist文件夹）
3. 是否有管理员权限运行
4. 防火墙是否阻止了程序

---

**祝使用愉快！** 🎉
